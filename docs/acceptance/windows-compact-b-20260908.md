# Windows 紧凑 B 界面验收

## 范围

用户确认横向紧凑 B：持续显示左侧状态与连接时长；仅保留左侧线路详情/返回主页；右侧主页只保留标题和主操作，详情为两列四行八目标。探测、连接服务、托盘行为不变。

浏览器稿窗口 480×260（含模拟标题栏）；Windows 使用 480×224 逻辑客户区，另加系统标题栏和边框。主页面与详情相同尺寸，系统 DPI 决定物理像素尺寸。

## 已执行验证

- 原生尺寸一致性测试 RED 0/2 → GREEN 2/2。
- Windows PowerShell 5.1 + Pester 3.4：204 通过、0 失败。PowerShell 7.6.5 的旧 Pester Should Throw 断言出现两项假阴性，显式使用 5.1 后通过；没有修改签名逻辑。
- 原生 CMake Release 构建成功，CTest 1/1 通过。
- Go 全量测试通过（报告缓存结果）；此次未修改 Go。
- 首轮 Flutter 82/82、analyze 无问题；视觉核对后修正主页居中和返回按钮对比度。
- 普通字号 280px 窄视口、八个 5000ms 结果复现八处横向溢出；修正为按详情面板实际宽度切换单列后通过，正常尺寸仍为两列四行。五字错误标题不再产生末字单独换行。
- 最终 Flutter 83/83、analyze 无问题；审查 `5ab381a..a2083c0` 经修正后批准，无待修项。主页、详情、未连接、连接中、不可用、配置错误和恢复失败等视觉基线已检查。

## 发布状态

首轮发布因本地依赖压缩包未位于发布器期望位置而停止，未生成正式 MSI、未改动测试机；纠正归档路径后发布成功。

- 版本 0.1.11，源提交 `a2083c0dd3acd3de0d002c0816e4f3fe00f4328b`，使用干净 LF checkout 构建。
- 安装包：`outputs/windows-compact-b-20260908/source/dist/OverseasAccessSetup-v0.1.11-poc-RELEASE_SIGNED.msi`。
- SHA256：`6401E22445EF56F2F831ACA389A545B5BF5FD0BE0814C0D0E3E1504A973EC6CA`。
- 发布脚本退出 0；29 项负载清单检查通过；SignTool `/pa /tw` 验证通过，包含 RFC3161 时间戳。WiX 保留既有 WIX1059/WIX1060 反编译提示，发布器 MSI 表和负载检查通过。
- 测试机 `172.20.20.200`：MSI 退出 0、安装后 payload verifier 退出 0；唯一注册版本 0.1.11，源提交匹配；服务 Running。
- 交互会话 2 启动已安装 GUI，PID 10572；一次性启动任务已删除。
- 升级前 API 为 idle，升级后仍为 idle（generation 1），未自动开启线路。API 收集任务退出 0。此次未重新执行实际外网站点探测，不将 UI 自动测试等同于线路实测。
- 受保护安装日志：`C:\ProgramData\RegenBio\PilotBackup-20260908\upgrade-0111.log`。旧版回滚备份保留；诊断模式未开启。

已提交开发分支，不包含合并主分支或推送 GitHub。实机视觉效果由用户继续确认；macOS/Linux 不属于本次部署结果。
