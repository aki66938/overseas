# Compact B Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development or superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Replace the Windows card UI with the user-approved compact B layout and deliver a signed test installer.

**Architecture:** Keep service/API/lifecycle state unchanged. Present one persistent left rail and switch the right content between home and details. Retain the native Windows caption and tray implementation.

**Tech Stack:** Flutter/Dart, Win32 runner, existing WiX/PowerShell signed release pipeline.

## Global Constraints

- Approved visual reference: `.superpowers/brainstorm/1193-1788856127/content/compact-b-refined.html`.
- Window target: 480 × 260 preview including its 36px caption; native content uses 480 × 224 logical pixels plus the existing system caption/frame. Home and details have the same size.
- Left rail 138 logical pixels, dark green #12382c; right white. No large circle, card nesting or duplicate connection status.
- Exactly one navigation action: left `线路详情`, becomes `返回主页` in details. No right `查看站点延迟` or duplicate back button.
- Home right contains `海外访问` and existing primary action only, except actionable existing configuration/service error guidance.
- Eight genuine probe results in two columns/four rows at normal text scale, all visible without scrolling. Preserve historical, unknown, error, busy and polling semantics; no invented results.
- No slow-site warning on home. Diagnostics remain off. No maximize; close/minimize/tray behavior unchanged.
- Enlarged text and narrow constraints may scroll/reflow without overflow; do not disable text scaling.
- Do not touch Go service/network logic, unrelated Linux drafts, or production infrastructure.

### Task 1: Compact Flutter presentation

**Files:** Modify `apps/regen_access/lib/app.dart`, `lib/pages/home.dart`, `lib/pages/details.dart`, `lib/theme.dart`; create `lib/pages/connection_rail.dart` if needed; update `test/pages_test.dart`, `test/goldens_test.dart` and golden images.

**Interfaces:** Preserve MyApp API and _DesktopState lifecycle operations. Presentation receives existing Status, DateTime, busy/polling and callbacks. A persistent rail must render the same authoritative status on both pages.

- [ ] Run baseline `flutter test` using `C:/Users/Eleme/codex_workspace/.tools/flutter-3.47.2/bin/flutter.bat`.
- [ ] Add failing tests for new logical shell size, persistent rail, unique navigation and four-row site layout. Update existing size/duration/back expectations to approved design, preserving lifecycle assertions.

```dart
expect(tester.getSize(find.byKey(const Key('desktop-shell'))), const Size(480, 224));
expect(find.text('查看站点延迟'), findsNothing);
await tester.tap(find.text('线路详情'));
await tester.pump();
expect(find.text('已连接'), findsOneWidget);
expect(find.text('返回主页'), findsOneWidget);
expect(tester.takeException(), isNull);
```

- [ ] Run focused tests to confirm expected RED, then implement layout from reference. Keep state derivation in one place rather than duplicating title/action logic.
- [ ] Normal geometry: rail padding 16px; content padding 12–20px; right header 13–20px; site rows about 33px, name 11px/status 9px/latency 12px. Compact buttons 28–36px, corners 5–6px.
- [ ] Test 480×224, 200% DPI, doubled text, narrow viewport and existing lifecycle suite. Generate new goldens only after functional tests pass; inspect actual images.
- [ ] Run `flutter analyze` and full `flutter test`, then commit only task files. Report RED/GREEN evidence and file list in `.superpowers/sdd/compact-b-report.md`.

### Task 2: Native dimensions and delivery

**Files:** `apps/regen_access/windows/runner/main.cpp`, `windows/runner/tests/smoke-owned-window.ps1`, acceptance documentation.

- [ ] Set native client `Win32Window::Size size(480, 224);` and smoke expected logical client dimensions accordingly.
- [ ] Review combined diff and run Windows release build/native tests; inspect Flutter goldens for home, details and error states.
- [ ] Commit verified changes, build signed version 0.1.11 from a clean LF checkout using existing publish-client-release.ps1 and existing approved test certificate/timestamp configuration.
- [ ] Verify Authenticode and manifest, deploy MSI only to 172.20.20.200 with existing SSH key and installation procedure. Confirm installed version, service status and GUI launch without changing network policy.
- [ ] Record evidence and installer path; report any unverified visual/runtime portions honestly. No merge/push in this task.
